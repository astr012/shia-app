# ============================================================
# SignAI_OS — Backend Dependencies
# ============================================================

# Core
fastapi>=0.110.0,<1.0.0
uvicorn[standard]>=0.27.0,<1.0.0
websockets>=12.0,<14.0
pydantic>=2.5.0,<3.0.0

# AI / NLP (optional — falls back gracefully if not available)
openai>=1.12.0,<2.0.0

# Premium TTS (optional)
# elevenlabs>=1.0.0

# Database (future)
# sqlalchemy>=2.0.0
# asyncpg>=0.29.0
# supabase>=2.0.0

# Utilities
python-dotenv>=1.0.0,<2.0.0
python-multipart>=0.0.6,<1.0.0
